<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mapper.CampusBoardMapper">


	<select id="list" resultType="com.spring.domain.CampusBoardVO">
		<![CDATA[
			select * 
				from (select rownum rn, b_no, b_title, b_writer, b_views, b_sort, b_sysdate
					from(select b_no,b_title,b_writer,b_views,b_sort,b_sysdate from campus_board where b_no>0 order by b_no desc) 
					where
		]]>
	
		<include refid="criteria"></include>
		
		<![CDATA[
				 rownum <= (#{page}*20)) 
			where rn > ((#{page}-1)*20)
		]]>
	</select>

	<delete id="delete">
		delete from campus_board where b_no = #{b_no}
	</delete>
	
	<update id="update">
		update campus_board set b_title = #{b_title}, b_content = #{b_content} where b_no = #{b_no}
	</update>

	
	<update id="addview">
		update campus_board set b_views = #{b_views} where b_no = #{b_no}
	</update>
	
	<!-- 전체 게시물 수 -->
	<select id="totalCnt" resultType="int">
		select count(*) from campus_board
		
		where
		
		<include refid="criteria"></include>
		
		b_no > 0
	</select>

	
	<select id="view" resultType="com.spring.domain.CampusBoardVO">
		select * from campus_board where b_no = #{b_no}
	</select>
	
	<!-- 댓글 숫자 수정 : 댓글의 삽입or삭제시 -->
	<update id="replyCntUpdate">
		update spring_board set replycnt = replycnt + 20 where bno = #{bno}
	</update>
	
	<insert id="insert">
		<!-- 주로 pk값을 미리 sql 쿼리문을 통해 처리해 두고 특정한 이름으로 결과를 보관 -->
		<selectKey keyProperty="b_no" order="BEFORE" resultType="int">
			select CAMPUS_BOARD_SEQUENCE.nextval from dual
		</selectKey>
		insert into campus_board(b_no, b_title, b_content, b_writer, b_sysdate, b_sort) values(#{b_no}, #{b_title}, #{b_content}, #{b_writer}, sysdate, #{b_sort})
	</insert>
	
	
	<!-- 영권 메인 게시판 10개 보여주기용 -->
	<select id="mainList" resultType="com.spring.domain.CampusBoardVO">
		<![CDATA[
			select * 
				from (select rownum rn, b_no, b_title, b_writer, b_views, b_sort, b_sysdate
					from(select b_no,b_title,b_writer,b_views,b_sort,b_sysdate from campus_board where b_no>0 order by b_no desc) 
					where
		]]>
				<trim prefix="(" suffix=") AND" prefixOverrides="OR">
					<foreach collection="sortArr" item='sort'>
						<trim prefix="OR">
							<choose>
								<when test="sort=='P'.toString()">
									b_sort like '%'||'상품'||'%'
								</when>
								<when test="sort=='Q'.toString()">
									b_sort like '%'||'질문'||'%'
								</when>
								<when test="sort=='R'.toString()">
									b_sort like '%'||'후기'||'%'
								</when>
							</choose>
						</trim>
					</foreach>
				</trim>
		
		<![CDATA[
				 rownum <= (#{page}*10)) 
			where rn > ((#{page}-1)*10)
		]]>
	</select>
	
	
	
	<!-- 동적 SQL -->
	<sql id = "criteria">
		<trim prefix="(" suffix=") AND" prefixOverrides="OR">
			<foreach collection="sortArr" item='sort'>
				<trim prefix="OR">
					<choose>
						<when test="sort=='T'.toString()">
							title like '%'||#{keyword}||'%'
						</when>
						<when test="sort=='C'.toString()">
							content like '%'||#{keyword}||'%'
						</when>
						<when test="sort=='W'.toString()">
							writer like '%'||#{keyword}||'%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>
	

</mapper>